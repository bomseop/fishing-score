# CLAUDE.md — 연안 워킹 조건 스코어

한국 연안 워킹 낚시(루어·원투)의 어종별 조건 적합도를 계산하는 개인용 앱.
Play 스토어 미등록, 본인과 지인 사이드로딩 배포.

---

## 1. 이 프로젝트가 하는 일

사용자가 "지금 이 포인트에서 이 어종, 갈까 말까"를 판단할 수 있게
기상·해양·법령 데이터를 모아 0~100점의 **조건 적합도 점수**를 낸다.

### 절대 혼동하지 말 것: 점수 ≠ 확률

표시값은 **입질 확률이 아니다.** 확률이라고 부르려면 "이 조건에서 몇 번
던져 몇 번 물었다"는 정답 라벨이 필요한데 그런 공개 데이터셋이 없다.
가중치는 어종 생태에 근거한 추정치이며 캘리브레이션되지 않았다.

UI 문구에 "확률", "확률(%)", "적중률" 같은 표현을 쓰지 말 것.
"조건 적합도", "점수", "판정"으로 통일한다.

---

## 2. 현재 파일 구조

```
www/index.html        프론트엔드 전체 (단일 파일, 런타임 의존성 없음)
index.html            루트 리다이렉트 → ./www/ (Pages 주소 유지용)
build_tides.py        KHOA 조석예보 → data/tide-<YEAR>.json
khoa_api.py           포털 API 공용 계층 (엔드포인트·인증키·지점표)
collect_sst.py        조위관측소 수온 → data/sst-latest.json
sync_regulations.py   법령 별표1·2   → data/regulations.json
capacitor.config.json Capacitor 설정 (.ts 아님 — TS 툴체인 불필요)
scripts/              폰트 번들 · 설정 주입 · android 패치 · 아이콘 생성
assets/               아이콘·스플래시 원본 PNG
.github/workflows/data.yml   6시간 주기 데이터 갱신
.github/workflows/apk.yml    태그 → 서명 APK → Releases
README.md             데이터 파이프라인 설명
SETUP.md              GitHub Pages + Actions 설치 절차
ANDROID.md            안드로이드 앱 설계 기록
```

호스팅: GitHub Pages (`https://<user>.github.io/<repo>/`)
데이터: Actions가 6시간마다 `data/`에 커밋 → Pages가 정적 서빙
앱: Capacitor 7 로 감싼 APK. `android/` 는 커밋하지 않고 CI가 매번 생성한 뒤
`scripts/patch-android.mjs` 로 패치한다. **안드로이드 설정의 단일 출처는 그 파일이다.**

웹과 앱은 같은 `www/index.html`을 공유한다. 분기는 `NATIVE` 상수 하나뿐이며
파일을 포크하지 않는다. 앱은 `CFG.REMOTE`(빌드 시 주입)로 Pages의 `data/`를
읽고, 웹은 상대경로 `../data/`로 읽는다.

---

## 3. 스코어링 모델 (핵심 명세)

```
Score = Gate_legal × Gate_region × Gate_season × Gate_temp × Gate_safety
        × Σ(wᵢ · fᵢ) × 100
```

### 게이트 — 곱셈. 하나라도 0이면 전체 0

| 게이트 | 조건 | 근거 |
|---|---|---|
| `legal` | 금어기 기간 | 법적으로 못 잡는데 점수가 뜨면 앱이 위법을 권하는 셈 |
| `region` | 해당 해역에 워킹으로 성립하지 않는 어종 | 동해 주꾸미 등 |
| `season` | 해당 월에 접안하지 않는 어종 | 생태적 부재 |
| `temp` | 어종별 치사수온 범위 이탈 | |
| `safety` | 파고 ≥3m, 또는 너울주기 ≥9s AND 너울높이 ≥1.5m | 높이보다 **주기**가 위험 지표 |

**금지체장은 게이트가 아니다.** 낚는 것 자체는 합법이고 미달 개체를
방생하면 되므로 판정에 영향을 주지 않고 수치로만 표시한다.

### 가중합 팩터 — 각 0~1 정규화, 어종별 가중치 다름

`temp` `tide` `light` `wave` `wind` `clarity` `structure`

### 물때 해역 계수 — 이 프로젝트의 핵심 인사이트

물때 가중치에 해역 계수를 곱한 뒤 전체를 재정규화한다.

| 해역 | 계수 | 근거 |
|---|---|---|
| 서해 | ×1.35 | 조차 6~9m, 조류가 베이트 이동을 지배 |
| 남해 | ×1.00 | 기준 |
| 동해 | ×0.35 | 조차 0.2~0.3m, 물때가 거의 무의미 |
| 제주 | ×0.85 | |

동해에서 물때표 붙잡고 고민하는 건 시간 낭비고, 서해에서 물때를 무시하면
조과 포기와 같다. 이 비대칭이 코드에 명시적으로 들어가 있다.

### 물때 팩터는 조위가 아니라 조위 변화율

고·저조 이벤트를 사인 보간해 변화율을 쓴다.

```
level(t) = (h₁+h₂)/2 + (h₁-h₂)/2 · cos(πτ)
|dh/dt|  ∝ sin(πτ)        정조=0, 중들물·중날물=최대
```

정조에서 입질이 끊기는 현상이 수식에서 그대로 나온다.
**절대 조위값으로 바꾸지 말 것.**

### 현재 규모

어종 22종 (루어 9 / 에깅 4 / 원투 6 / 찌 3)
포인트 41개소 (동해 13 / 서해 15 / 남해 10 / 제주 3) — 전부 공공 지도 등재 항구·해변

---

## 4. 데이터 소스와 폴백

| 항목 | 소스 | 방식 | 폴백 |
|---|---|---|---|
| 파고·너울주기·바람·기압·일출몰 | Open-Meteo | 클라이언트 직접 호출 | 없음 (필수) |
| 격자 해면수온 | Open-Meteo Marine | 클라이언트 직접 호출 | — |
| 조석 | data.go.kr 1192136 `tideFcstHghLw` | 연 1회 선계산 정적 JSON | 달 위상 근사 |
| 연안 실측 수온 | data.go.kr 1192136 `dtRecent` | 6시간 주기 정적 JSON | Open-Meteo 격자 |
| 금어기·금지체장 | 국가법령정보센터 OPEN API | 시행일 변경 시 재수집 | 내장 씨드값 |

**Open-Meteo는 인증키 없음 + CORS 허용 + 다중 좌표 배치 요청 지원.**
41개 포인트 전체가 요청 2회로 끝난다. 이 특성을 유지할 것.

**구 바다누리 OpenAPI(`khoa.go.kr/api/oceangrid`)는 2026-04-01 자로 종료됐다.**
조석·수온은 공공데이터포털(data.go.kr) 기관코드 1192136 으로 이전됐고, 인증키는
계정당 하나(`DATA_GO_KR_KEY`)라 두 API가 같은 값을 쓴다. 엔드포인트 정의는
`khoa_api.py` 한 곳에 모여 있다 — 수집기가 직접 URL을 갖지 않는다.

**포털 API와 법령은 CORS가 막혀 있다.** 브라우저에서 직접 못 부른다.
그래서 수집기가 정적 JSON을 떨구고 프론트가 읽는다. 백엔드 서버 없음.

### 신선도 검사

수집기가 죽었는데 오래된 값을 정상인 척 보여주는 게 가장 위험하다.
수온 12시간, 법령 150일이 지나면 피드 배지가 `갱신중단 N일`로 바뀐다.
**이 로직을 제거하지 말 것.**

---

## 5. 법령 데이터 주의사항

내장 씨드값은 2026.1.1 기준으로 정리한 **참고값이며 법적 효력이 없다.**
`sync_regulations.py`를 돌려야 현행 별표로 갱신된다.
씨드 상태일 때 UI에 `미검증`이 표시되는 로직을 유지할 것.

### 이 파이프라인이 못 잡는 것

- **시·도 조례** — 참문어 금어기처럼 관할 지자체장이 조정 가능한 항목
- **한시적 유예** — TAC 대상 어종 유예는 중앙수산조정위원회 고시로 나가며 별표에 없음
- **비어업인 특례** — 낚시인에게만 적용되는 어구·어법 제한

그래서 법규 밴드에 해양수산부 고시 링크가 상시 노출된다. **제거 금지.**

---

## 6. 디자인 언어

해도(nautical chart) 계열. 다크 계열이되 순검정이 아니라 청록 편향 심해색.

```
--abyss  #071A21   --deep  #0D2A33   --shelf #14424C   --shoal #1E6470
--sand   #EFE3C6   --magenta #FF2E6E (강조·위험)
--aqua   #46E0C8   --amber #FFB23D
```

폰트: Pretendard(본문) + JetBrains Mono(모든 수치·라벨).
계기판 느낌이 이 앱의 정체성이다.

**시그니처 요소 — 사운딩 컬럼.** 팩터별 가로 막대에서
**트랙 길이 = 가중치, 채움 = 현재값.** 곱셈 구조가 눈에 보인다.
이걸 일반 프로그레스바로 바꾸지 말 것.

---

## 7. 하지 말 것

- "확률" 표현 사용
- 물때 팩터를 조위 절대값으로 변경
- 해역 계수 제거 또는 균일화
- 금어기를 게이트에서 표시로 강등
- 금지체장을 게이트로 승격
- 신선도 경고 로직 제거
- 법령 고시 링크 제거
- localStorage/sessionStorage 사용 (Capacitor Preferences/Filesystem 사용)
- 조과 로그 기능 추가 (사용자가 명시적으로 제외 요청함)
- KHOA·NIFS·법령 인증키를 앱에서 직접 사용 (APK 디컴파일 시 그대로 노출)
- 위치 권한 추가 (GPS 최근접 선택은 만들었다가 제거했다 — ANDROID.md 6-2)
- `android/` 디렉터리를 커밋 (CI 생성물과 이중 관리가 되어 어긋난다)
- `www/index.html`을 앱용·웹용으로 포크
- 캐시에서 꺼낸 값에 신선도 검사를 건너뛰기 (오프라인은 면제부가 아니다)
- Open-Meteo 캐시를 `DATA.omOff` 보정 없이 사용 (날짜가 바뀌면 24칸 밀린 값을
  오늘 것인 양 보여준다)

---

## 8. 향후 확장 후보

- 기상특보 게이트 — 기상청 `WthrWrnInfoService`로 풍랑주의보·경보를 받아
  해당 해역 전체 차단. 현재 파고·너울 기준보다 한 겹 위 방어선
- 포인트 `terrain` 태깅 정밀화 — 간조 시각 위성사진 판독이 가장 정확.
  `structure` 가중치가 최대 30%라 태깅 오류가 점수를 크게 흔든다
- 가중치 튜닝 — `score()`가 순수 함수라 과거 조건 벡터만 있으면
  백테스트·그리드서치가 그대로 된다
